function HeroSection() {
  return (
    <section className="hero-card">
      <p className="eyebrow">MSE-1 Frontend</p>
      <h1>Inventory Management Dashboard</h1>
      <p className="hero-copy">
        React frontend built from the existing Express and MongoDB backend. It supports
        product creation, updates, deletion, search, and category filtering.
      </p>
    </section>
  );
}

export default HeroSection;
