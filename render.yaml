services:
  - type: web
    name: inventory-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
